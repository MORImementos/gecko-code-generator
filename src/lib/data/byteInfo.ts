import byteStatOffset from './byteStatOffset.json'
import charInfo from './charInfo.json'
import chemOffset from './chemOffset.json'
import { charName } from '$lib/helpers/helpers'

export const byteInfo = {
    byteStatOffset: byteStatOffset,
    charInfo: charInfo,
    chemOffset: chemOffset,
    charName: charName,
}